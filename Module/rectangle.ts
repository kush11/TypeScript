export default function calculateRectangle(width: number, length: number): number {
    return width * length;
}

export function calculateRectangle1(width: number, length: number): number {
    return width * length;
}